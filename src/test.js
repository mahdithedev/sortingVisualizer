import boubleSort from "./functions/sort";

let array = []
      for(let i=0 ; i<size ; i++)
        {
          const number = Math.floor(Math.random() * 295 ) + 5
          const left = i * 20 + "px"
          const color = "crimson"
          array.push({number,left})
        }


console.log(boubleSort)